export interface RequestLog{
    id: number
    createdDate: string
    ip: string
    address: string
    host: string
    referer: string
    userAgent: string
    method: string
    requestUri: string
    sessionId: string
    clientId: string
    userId: number
}
